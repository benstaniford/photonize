<Window x:Class="Photonize.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dd="urn:gong-wpf-dragdrop"
        xmlns:viewModels="clr-namespace:Photonize.ViewModels"
        xmlns:helpers="clr-namespace:Photonize.Helpers"
        Title="Photonize" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        BorderBrush="#FF2A2A2A"
        BorderThickness="1">

    <Window.Resources>
        <helpers:PhotoDropHandler x:Key="PhotoDropHandler"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="F5" Command="{Binding RefreshCommand}"/>
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{Binding BrowseDirectoryCommand}"/>
        <KeyBinding Key="Delete" Command="{Binding DeletePhotoCommand}"/>
    </Window.InputBindings>

    <Grid Background="#FF1E1E1E">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="{Binding PreviewColumnWidth}" MinWidth="0"/>
        </Grid.ColumnDefinitions>

        <!-- Main Content Area -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

        <!-- Top Section: Directory Path -->
        <GroupBox Header="Directory" Grid.Row="0" Margin="10,10,10,5">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         x:Name="DirectoryPathTextBox"
                         Text="{Binding DirectoryPath, UpdateSourceTrigger=LostFocus}"
                         VerticalContentAlignment="Center"
                         Padding="5"
                         FontSize="14"
                         PreviewKeyDown="DirectoryPathTextBox_PreviewKeyDown"/>

                <Button Grid.Column="1"
                        Content="Browse..."
                        Command="{Binding BrowseDirectoryCommand}"
                        Padding="15,5"
                        Margin="5,0,0,0"
                        FontSize="14"/>

                <Button Grid.Column="2"
                        Content="↻"
                        Command="{Binding LoadPhotosCommand}"
                        Padding="10,5"
                        Margin="5,0,0,0"
                        FontSize="20"
                        ToolTip="Refresh (F5)"
                        MinWidth="40"/>
            </Grid>
        </GroupBox>

        <!-- Rename Prefix -->
        <GroupBox Header="Rename Prefix" Grid.Row="1" Margin="10,5,10,5">
            <TextBox Text="{Binding RenamePrefix, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center"
                     Padding="5"
                     Margin="5"
                     FontSize="14"
                     ToolTip="Enter a prefix for renamed files (e.g., 'vacation' will create vacation-00001.jpg, vacation-00002.jpg, etc.)"/>
        </GroupBox>

        <!-- Thumbnail Size Slider -->
        <GroupBox Header="Thumbnail Size" Grid.Row="2" Margin="10,5,10,5">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Slider Grid.Column="0"
                        Minimum="100"
                        Maximum="400"
                        Value="{Binding ThumbnailSize}"
                        TickFrequency="50"
                        IsSnapToTickEnabled="True"
                        VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1"
                           Text="{Binding ThumbnailSize, StringFormat={}{0:F0} px}"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"
                           FontSize="14"
                           MinWidth="60"/>

                <Button Grid.Column="2"
                        Content="👁️"
                        Command="{Binding TogglePreviewCommand}"
                        Padding="10,5"
                        FontSize="18"
                        Margin="10,0,0,0"
                        MinWidth="45"
                        ToolTip="Toggle Preview"
                        VerticalAlignment="Center">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPreviewVisible}" Value="True">
                                    <Setter Property="Background" Value="#FF007ACC"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </GroupBox>

        <!-- Photos Grid -->
        <GroupBox Header="Photos" Grid.Row="3" Margin="10,5,10,5">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto"
                          x:Name="PhotoScrollViewer"
                          PreviewMouseWheel="PhotoScrollViewer_PreviewMouseWheel">
                <ListBox ItemsSource="{Binding Photos}"
                         SelectionMode="Extended"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                         dd:DragDrop.IsDragSource="True"
                         dd:DragDrop.IsDropTarget="True"
                         dd:DragDrop.DropHandler="{StaticResource PhotoDropHandler}"
                         dd:DragDrop.UseDefaultDragAdorner="True"
                         dd:DragDrop.DefaultDragAdornerOpacity="0.5"
                         x:Name="PhotoListBox">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="ItemBorder"
                                                Background="Transparent"
                                                BorderBrush="Transparent"
                                                BorderThickness="2"
                                                CornerRadius="4">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#FF007ACC"/>
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#33007ACC"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5"
                                    Padding="5"
                                    ToolTip="{Binding FilePath}"
                                    Cursor="Hand"
                                    Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Window}}">
                                <Border.InputBindings>
                                    <MouseBinding MouseAction="LeftDoubleClick"
                                                  Command="{Binding DataContext.OpenPhotoCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                  CommandParameter="{Binding}"/>
                                </Border.InputBindings>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource PhotoBorderStyle}">
                                        <Setter Property="ContextMenu">
                                            <Setter.Value>
                                                <ContextMenu>
                                                    <MenuItem Header="Show in Explorer"
                                                              Command="{Binding PlacementTarget.Tag.ShowInExplorerCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                                        <MenuItem.Icon>
                                                            <TextBlock Text="📁" FontSize="14"/>
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                    <MenuItem Header="Export WebP"
                                                              Command="{Binding PlacementTarget.Tag.ExportWebPCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                                        <MenuItem.Icon>
                                                            <TextBlock Text="🖼️" FontSize="14"/>
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                    <Separator/>
                                                    <MenuItem Header="Delete"
                                                              Command="{Binding PlacementTarget.Tag.DeletePhotoCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                                        <MenuItem.Icon>
                                                            <TextBlock Text="🗑️" FontSize="14"/>
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                </ContextMenu>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsFolder}" Value="True">
                                                <Setter Property="ContextMenu" Value="{x:Null}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <Image Source="{Binding Thumbnail}"
                                           Width="{Binding DataContext.ThumbnailSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                           Height="{Binding DataContext.ThumbnailSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                           Stretch="Uniform"/>
                                    <TextBlock Text="{Binding FileName}"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap"
                                               MaxWidth="{Binding DataContext.ThumbnailSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                               Margin="0,5,0,0"
                                               FontSize="11"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </GroupBox>

            <!-- Apply Button -->
            <Button Grid.Row="4"
                    Content="Apply Rename"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding ApplyRenameCommand}"
                    Padding="20,10"
                    Margin="10,5,10,5"
                    FontSize="16"
                    FontWeight="Bold"
                    HorizontalAlignment="Center"/>

            <!-- Status Bar -->
            <StatusBar Grid.Row="5">
                <StatusBarItem>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="12"/>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem>
                    <TextBlock Text="{Binding Photos.Count, StringFormat=Photos: {0}}" FontSize="12"/>
                </StatusBarItem>
                <StatusBarItem HorizontalAlignment="Right">
                    <ProgressBar Width="100"
                                 Height="16"
                                 IsIndeterminate="True"
                                 Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"/>
                </StatusBarItem>
            </StatusBar>
        </Grid>

        <!-- GridSplitter -->
        <GridSplitter Grid.Column="1"
                      Width="5"
                      HorizontalAlignment="Stretch"
                      Background="#FF3E3E42"
                      Visibility="{Binding IsPreviewVisible, Converter={StaticResource BoolToVisConverter}}"
                      ShowsPreview="True"
                      Cursor="SizeWE">
            <GridSplitter.Style>
                <Style TargetType="GridSplitter">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF007ACC"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </GridSplitter.Style>
        </GridSplitter>

        <!-- Preview Pane -->
        <Border Grid.Column="2"
                Background="#FF252526"
                BorderBrush="#FF3E3E42"
                BorderThickness="1,0,0,0"
                Visibility="{Binding IsPreviewVisible, Converter={StaticResource BoolToVisConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Preview Header -->
                <Border Grid.Row="0"
                        Background="#FF2D2D30"
                        BorderBrush="#FF3E3E42"
                        BorderThickness="0,0,0,1"
                        Padding="10">
                    <TextBlock Text="Preview"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#FFF1F1F1"/>
                </Border>

                <!-- Preview Image -->
                <Grid Grid.Row="1">
                    <Image Source="{Binding PreviewImage}"
                           Stretch="Uniform"
                           Margin="10"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>

                    <!-- No Image Selected Text -->
                    <TextBlock Text="No image selected"
                               FontSize="14"
                               Foreground="#FF808080"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding PreviewPhoto}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
